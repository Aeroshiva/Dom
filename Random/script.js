// let a= document.images;
// Array.from(a).forEach((element)=>{
//     console.log(element);
// });

// let b =document.links;
// var string = "mozilla";

// Array.from(b).forEach((element)=>{
// if (element.href.includes(string))
//     console.log(element.href);
// });

let element = document.getElementById('ram');
//element = element.classList;
//element = element.parentElement;
//element.innerHTML += 'we are the best';
//a = element.innerText +='best in the busineess';
console.log(element);
